# Cloudflare Pages project configuration
# Name of the project in the Cloudflare Pages dashboard
name = "cronyx-labs"

# Directory containing the static production build emitted by Vite (`bun run build`)
pages_build_output_dir = "dist"

# Compatibility date determining Cloudflare runtime behaviour
compatibility_date = "2026-09-01"
